services:
  openclaw-gateway:
    environment:
      GH_TOKEN: ${GH_TOKEN}
      GITHUB_TOKEN: ${GITHUB_TOKEN}
    volumes:
      - ./:/host-openclaw

  openclaw-cli:
    environment:
      GH_TOKEN: ${GH_TOKEN}
      GITHUB_TOKEN: ${GITHUB_TOKEN}
    volumes:
      - ./:/host-openclaw

